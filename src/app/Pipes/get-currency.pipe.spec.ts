import { GetCurrencyPipe } from './get-currency.pipe';

describe('GetCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
