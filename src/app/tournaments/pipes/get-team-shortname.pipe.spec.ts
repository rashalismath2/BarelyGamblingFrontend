import { GetTeamShortnamePipe } from './get-team-shortname.pipe';

describe('GetTeamShortnamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetTeamShortnamePipe();
    expect(pipe).toBeTruthy();
  });
});
